---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: pdinferenceservices.pdai.pdai.io
spec:
  group: pdai.pdai.io
  names:
    kind: PDInferenceService
    listKind: PDInferenceServiceList
    plural: pdinferenceservices
    shortNames:
    - pdis
    singular: pdinferenceservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PDInferenceService is the Schema for the pdinferenceservices
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PDInferenceServiceSpec defines the desired state of PDInferenceService.
            properties:
              decode:
                description: Decode configures the decode role.
                properties:
                  args:
                    description: |-
                      Args are the complete startup arguments passed to the inference container.
                      pd-manager does not auto-inject engine args; all args must be specified here
                      or supplied by the referenced engineProfileRef.
                      The environment variable $(POD_IP) is available for use in args.
                    items:
                      type: string
                    type: array
                  engineRuntimes:
                    description: |-
                      EngineRuntimes injects sidecar containers into this role's pods via RBG engineRuntimes.
                      Used for platform integrations such as Patio worker registration.
                    items:
                      description: |-
                        EngineRuntime maps to an RBG engineRuntime entry, enabling platform-specific
                        container arg overrides. pd-manager passes this through without interpreting it.
                      properties:
                        containers:
                          items:
                            description: |-
                              RuntimeContainer describes a single container override in an EngineRuntime.
                              The args are passed through to the RBG engineRuntimes field without parsing.
                            properties:
                              args:
                                items:
                                  type: string
                                type: array
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                        profileName:
                          type: string
                      required:
                      - profileName
                      type: object
                    type: array
                  gpu:
                    description: GPU is the number of GPU devices to allocate per
                      pod (e.g. "2").
                    type: string
                  gpuType:
                    description: |-
                      GPUType constrains scheduling to nodes with the matching "accelerator" label
                      (e.g. "a30", "h100").
                    type: string
                  image:
                    description: |-
                      Image is the container image for this role.
                      Required when engineProfileRef is not set.
                    type: string
                  livenessProbe:
                    description: LivenessProbe configures the liveness health check.
                    properties:
                      failureThreshold:
                        description: FailureThreshold is the minimum consecutive failures
                          for the probe to be considered failed.
                        format: int32
                        type: integer
                      httpPath:
                        description: HTTPPath is the HTTP endpoint to probe. Defaults
                          to "/health".
                        type: string
                      initialDelaySeconds:
                        description: InitialDelaySeconds is the number of seconds
                          after the container starts before probing begins.
                        format: int32
                        type: integer
                      periodSeconds:
                        description: PeriodSeconds is how often (in seconds) to perform
                          the probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to probe. Defaults to 8000.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: TimeoutSeconds is the number of seconds after
                          which the probe times out.
                        format: int32
                        type: integer
                    type: object
                  readinessProbe:
                    description: ReadinessProbe configures the readiness health check.
                    properties:
                      failureThreshold:
                        description: FailureThreshold is the minimum consecutive failures
                          for the probe to be considered failed.
                        format: int32
                        type: integer
                      httpPath:
                        description: HTTPPath is the HTTP endpoint to probe. Defaults
                          to "/health".
                        type: string
                      initialDelaySeconds:
                        description: InitialDelaySeconds is the number of seconds
                          after the container starts before probing begins.
                        format: int32
                        type: integer
                      periodSeconds:
                        description: PeriodSeconds is how often (in seconds) to perform
                          the probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to probe. Defaults to 8000.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: TimeoutSeconds is the number of seconds after
                          which the probe times out.
                        format: int32
                        type: integer
                    type: object
                  replicas:
                    description: Replicas is the number of instances for this role.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  resources:
                    description: |-
                      Resources defines CPU and memory requests and limits.
                      GPU resources are specified via the GPU field.
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: Limits is the maximum resources allowed.
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        description: Requests is the minimum resources required.
                        type: object
                    type: object
                  volumeMounts:
                    description: VolumeMounts lists volume mounts referencing volumes
                      defined in spec.volumes.
                    items:
                      description: VolumeMountSpec references a named volume from
                        spec.volumes and specifies where to mount it.
                      properties:
                        mountPath:
                          description: MountPath is the path inside the container
                            where the volume is mounted.
                          type: string
                        name:
                          description: Name must match a volume defined in spec.volumes.
                          type: string
                        readOnly:
                          description: ReadOnly mounts the volume as read-only. Defaults
                            to false.
                          type: boolean
                      required:
                      - mountPath
                      - name
                      type: object
                    type: array
                required:
                - replicas
                type: object
              engine:
                default: sglang
                description: Engine selects the inference engine. Defaults to sglang.
                enum:
                - sglang
                type: string
              engineProfileRef:
                description: |-
                  EngineProfileRef names a PDEngineProfile in the same namespace
                  to use as the base configuration template (images and per-role args).
                type: string
              model:
                description: |-
                  Model is the model identifier (e.g. "Qwen/Qwen3-14B").
                  Used for labeling and as the served-model-name in inference APIs.
                type: string
              pdRatio:
                description: |-
                  PDRatio is the desired prefill:decode replica ratio (e.g. "1:2").
                  Mutually exclusive with scaling.prefill.
                type: string
              prefill:
                description: Prefill configures the prefill role.
                properties:
                  args:
                    description: |-
                      Args are the complete startup arguments passed to the inference container.
                      pd-manager does not auto-inject engine args; all args must be specified here
                      or supplied by the referenced engineProfileRef.
                      The environment variable $(POD_IP) is available for use in args.
                    items:
                      type: string
                    type: array
                  engineRuntimes:
                    description: |-
                      EngineRuntimes injects sidecar containers into this role's pods via RBG engineRuntimes.
                      Used for platform integrations such as Patio worker registration.
                    items:
                      description: |-
                        EngineRuntime maps to an RBG engineRuntime entry, enabling platform-specific
                        container arg overrides. pd-manager passes this through without interpreting it.
                      properties:
                        containers:
                          items:
                            description: |-
                              RuntimeContainer describes a single container override in an EngineRuntime.
                              The args are passed through to the RBG engineRuntimes field without parsing.
                            properties:
                              args:
                                items:
                                  type: string
                                type: array
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                        profileName:
                          type: string
                      required:
                      - profileName
                      type: object
                    type: array
                  gpu:
                    description: GPU is the number of GPU devices to allocate per
                      pod (e.g. "2").
                    type: string
                  gpuType:
                    description: |-
                      GPUType constrains scheduling to nodes with the matching "accelerator" label
                      (e.g. "a30", "h100").
                    type: string
                  image:
                    description: |-
                      Image is the container image for this role.
                      Required when engineProfileRef is not set.
                    type: string
                  livenessProbe:
                    description: LivenessProbe configures the liveness health check.
                    properties:
                      failureThreshold:
                        description: FailureThreshold is the minimum consecutive failures
                          for the probe to be considered failed.
                        format: int32
                        type: integer
                      httpPath:
                        description: HTTPPath is the HTTP endpoint to probe. Defaults
                          to "/health".
                        type: string
                      initialDelaySeconds:
                        description: InitialDelaySeconds is the number of seconds
                          after the container starts before probing begins.
                        format: int32
                        type: integer
                      periodSeconds:
                        description: PeriodSeconds is how often (in seconds) to perform
                          the probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to probe. Defaults to 8000.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: TimeoutSeconds is the number of seconds after
                          which the probe times out.
                        format: int32
                        type: integer
                    type: object
                  readinessProbe:
                    description: ReadinessProbe configures the readiness health check.
                    properties:
                      failureThreshold:
                        description: FailureThreshold is the minimum consecutive failures
                          for the probe to be considered failed.
                        format: int32
                        type: integer
                      httpPath:
                        description: HTTPPath is the HTTP endpoint to probe. Defaults
                          to "/health".
                        type: string
                      initialDelaySeconds:
                        description: InitialDelaySeconds is the number of seconds
                          after the container starts before probing begins.
                        format: int32
                        type: integer
                      periodSeconds:
                        description: PeriodSeconds is how often (in seconds) to perform
                          the probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to probe. Defaults to 8000.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: TimeoutSeconds is the number of seconds after
                          which the probe times out.
                        format: int32
                        type: integer
                    type: object
                  replicas:
                    description: Replicas is the number of instances for this role.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  resources:
                    description: |-
                      Resources defines CPU and memory requests and limits.
                      GPU resources are specified via the GPU field.
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: Limits is the maximum resources allowed.
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        description: Requests is the minimum resources required.
                        type: object
                    type: object
                  volumeMounts:
                    description: VolumeMounts lists volume mounts referencing volumes
                      defined in spec.volumes.
                    items:
                      description: VolumeMountSpec references a named volume from
                        spec.volumes and specifies where to mount it.
                      properties:
                        mountPath:
                          description: MountPath is the path inside the container
                            where the volume is mounted.
                          type: string
                        name:
                          description: Name must match a volume defined in spec.volumes.
                          type: string
                        readOnly:
                          description: ReadOnly mounts the volume as read-only. Defaults
                            to false.
                          type: boolean
                      required:
                      - mountPath
                      - name
                      type: object
                    type: array
                required:
                - replicas
                type: object
              router:
                description: Router configures the router role (sgl-router / sgl-model-gateway).
                properties:
                  args:
                    description: |-
                      Args are the complete startup arguments passed to the router container.
                      Required (either here or via engineProfileRef).
                    items:
                      type: string
                    type: array
                  image:
                    description: |-
                      Image is the container image for the router.
                      Required when engineProfileRef is not set.
                    type: string
                  livenessProbe:
                    description: LivenessProbe configures the liveness health check
                      for the router.
                    properties:
                      failureThreshold:
                        description: FailureThreshold is the minimum consecutive failures
                          for the probe to be considered failed.
                        format: int32
                        type: integer
                      httpPath:
                        description: HTTPPath is the HTTP endpoint to probe. Defaults
                          to "/health".
                        type: string
                      initialDelaySeconds:
                        description: InitialDelaySeconds is the number of seconds
                          after the container starts before probing begins.
                        format: int32
                        type: integer
                      periodSeconds:
                        description: PeriodSeconds is how often (in seconds) to perform
                          the probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to probe. Defaults to 8000.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: TimeoutSeconds is the number of seconds after
                          which the probe times out.
                        format: int32
                        type: integer
                    type: object
                  readinessProbe:
                    description: ReadinessProbe configures the readiness health check
                      for the router.
                    properties:
                      failureThreshold:
                        description: FailureThreshold is the minimum consecutive failures
                          for the probe to be considered failed.
                        format: int32
                        type: integer
                      httpPath:
                        description: HTTPPath is the HTTP endpoint to probe. Defaults
                          to "/health".
                        type: string
                      initialDelaySeconds:
                        description: InitialDelaySeconds is the number of seconds
                          after the container starts before probing begins.
                        format: int32
                        type: integer
                      periodSeconds:
                        description: PeriodSeconds is how often (in seconds) to perform
                          the probe.
                        format: int32
                        type: integer
                      port:
                        description: Port is the port to probe. Defaults to 8000.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        description: TimeoutSeconds is the number of seconds after
                          which the probe times out.
                        format: int32
                        type: integer
                    type: object
                  replicas:
                    description: Replicas is the number of router instances. Defaults
                      to 1.
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: Resources defines CPU and memory requests and limits
                      for the router container.
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: Limits is the maximum resources allowed.
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        description: Requests is the minimum resources required.
                        type: object
                    type: object
                  volumeMounts:
                    description: VolumeMounts lists volume mounts referencing volumes
                      defined in spec.volumes.
                    items:
                      description: VolumeMountSpec references a named volume from
                        spec.volumes and specifies where to mount it.
                      properties:
                        mountPath:
                          description: MountPath is the path inside the container
                            where the volume is mounted.
                          type: string
                        name:
                          description: Name must match a volume defined in spec.volumes.
                          type: string
                        readOnly:
                          description: ReadOnly mounts the volume as read-only. Defaults
                            to false.
                          type: boolean
                      required:
                      - mountPath
                      - name
                      type: object
                    type: array
                type: object
              scaling:
                description: Scaling configures HPA-based autoscaling.
                properties:
                  decode:
                    description: HPASpec defines Horizontal Pod Autoscaler configuration
                      for a role.
                    properties:
                      maxReplicas:
                        format: int32
                        type: integer
                      minReplicas:
                        format: int32
                        type: integer
                    required:
                    - maxReplicas
                    type: object
                  prefill:
                    description: HPASpec defines Horizontal Pod Autoscaler configuration
                      for a role.
                    properties:
                      maxReplicas:
                        format: int32
                        type: integer
                      minReplicas:
                        format: int32
                        type: integer
                    required:
                    - maxReplicas
                    type: object
                type: object
              volumes:
                description: Volumes defines shared volumes available to all roles
                  via volumeMounts.
                items:
                  description: |-
                    VolumeSpec defines a volume that can be shared across roles.
                    Exactly one of hostPath, emptyDir, or pvc must be set.
                  properties:
                    emptyDir:
                      description: EmptyDir creates an ephemeral directory (optionally
                        in-memory).
                      properties:
                        medium:
                          description: Medium is the storage medium (e.g. "Memory").
                            Empty string means default (disk).
                          type: string
                        sizeLimit:
                          description: SizeLimit is the maximum size of the volume
                            (e.g. "20Gi"). Optional.
                          type: string
                      type: object
                    hostPath:
                      description: HostPath mounts a host directory.
                      properties:
                        path:
                          description: Path is the node-local directory path.
                          type: string
                        type:
                          description: Type is the hostPath type (e.g. "Directory",
                            "File"). Optional.
                          type: string
                      required:
                      - path
                      type: object
                    name:
                      description: Name is the volume name referenced by volumeMounts.
                      type: string
                    pvc:
                      description: PVC mounts a PersistentVolumeClaim.
                      properties:
                        claimName:
                          description: ClaimName is the name of the PersistentVolumeClaim.
                          type: string
                      required:
                      - claimName
                      type: object
                  required:
                  - name
                  type: object
                type: array
            required:
            - decode
            - model
            - prefill
            - router
            type: object
          status:
            description: PDInferenceServiceStatus defines the observed state of PDInferenceService.
            properties:
              conditions:
                description: Conditions holds standard Kubernetes condition objects.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpoint:
                description: Endpoint is the address where inference requests can
                  be sent.
                type: string
              lastEvent:
                description: LastEvent records the most recent notable event.
                properties:
                  message:
                    type: string
                  reason:
                    type: string
                type: object
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Initializing
                - Running
                - Failed
                - Terminating
                type: string
              roleStatuses:
                description: RoleStatuses reports readiness per role.
                items:
                  description: RoleStatus reports the observed state of a single role.
                  properties:
                    name:
                      type: string
                    ready:
                      format: int32
                      type: integer
                    total:
                      format: int32
                      type: integer
                  required:
                  - name
                  - ready
                  - total
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
